{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://maoperatingsystem.com/schemas/regulated-agentic-ai-inventory.schema.json",
  "title": "Regulated Agentic AI Product Inventory",
  "description": "Schema for a simple function-first inventory of AI-enabled products for Agentic and MCP-enabled regulated-industry ecosystems.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "inventory_version",
    "inventory_name",
    "products"
  ],
  "properties": {
    "inventory_version": {
      "type": "string"
    },
    "inventory_name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$"
    },
    "description": {
      "type": "string"
    },
    "products": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/product"
      }
    }
  },
  "$defs": {
    "product": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "product_name",
        "display_name",
        "domain",
        "description",
        "reference_implementations",
        "capabilities"
      ],
      "properties": {
        "product_name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$",
          "description": "Canonical single-word lowercase function name, such as gateway, retrieval or audit."
        },
        "display_name": {
          "type": "string"
        },
        "domain": {
          "type": "string",
          "enum": [
            "experience",
            "agentic",
            "knowledge",
            "integration",
            "governance",
            "operations"
          ]
        },
        "description": {
          "type": "string"
        },
        "reference_implementations": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/reference_implementation"
          }
        },
        "capabilities": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/capability"
          }
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$"
          }
        }
      }
    },
    "reference_implementation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "implementation_type"
      ],
      "properties": {
        "name": {
          "type": "string"
        },
        "vendor": {
          "type": "string"
        },
        "implementation_type": {
          "type": "string",
          "enum": [
            "commercial",
            "open_source",
            "cloud_service",
            "mcp_server",
            "internal",
            "framework"
          ]
        },
        "deployment_models": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "saas",
              "private_cloud",
              "on_prem",
              "self_hosted",
              "managed_cloud",
              "embedded",
              "api"
            ]
          }
        },
        "url": {
          "type": "string"
        },
        "notes": {
          "type": "string"
        }
      }
    },
    "capability": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "capability_name",
        "display_name",
        "description"
      ],
      "properties": {
        "capability_name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$"
        },
        "display_name": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "regulated_relevance": {
          "type": "string"
        },
        "required": {
          "type": "boolean"
        }
      }
    }
  }
}